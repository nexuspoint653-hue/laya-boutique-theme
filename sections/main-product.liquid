{% render 'section-style', s: section %}
{% render 'section-bg', s: section %}
{% render 'section-copy', s: section, place: 'top' %}
{%- assign v = product.selected_or_first_available_variant -%}
<div class="wrap" style="padding-top:34px">
  <div class="crumb"><a href="{{ routes.root_url }}">Home</a> &nbsp;/&nbsp; {%- if collection %}<a href="{{ collection.url }}">{{ collection.title }}</a> &nbsp;/&nbsp; {% endif -%}{{ product.title }}</div>
</div>

<div class="wrap" style="padding-top:18px;padding-bottom:clamp(50px,6vw,90px)">
  <div class="pdp">
    {%- liquid
      assign gal_style = section.settings.gallery_style | default: 'spotlight'
      assign stage_ratio = section.settings.stage_ratio | default: '4-5'
      assign mosaic_pos = section.settings.mosaic_pos | default: 'tr'
      assign mosaic_w = section.settings.mosaic_width | default: 138
      assign motion_place = section.settings.motion_place | default: 'below'
      assign motion_w = section.settings.motion_width | default: 46
      assign img_limit = section.settings.image_limit | default: 0
      if img_limit == 0
        assign img_limit = 12
      endif

      case stage_ratio
        when '1-1'
          assign stage_pad = '100%'
        when '4-5'
          assign stage_pad = '125%'
        when '3-4'
          assign stage_pad = '133.33%'
        when '2-3'
          assign stage_pad = '150%'
        else
          assign stage_pad = '125%'
      endcase
    -%}

    {%- comment -%}
      Split the media: stills carry the spotlight, film goes to its own smaller
      card. A two-piece set ships three stills and a film, an abaya can ship
      four and no film — the same markup covers both without a switch.
    {%- endcomment -%}
    {%- assign stills = product.media | where: 'media_type', 'image' -%}
    {%- assign films = product.media | where: 'media_type', 'video' -%}
    {%- assign ext_films = product.media | where: 'media_type', 'external_video' -%}

    {%- if gal_style == 'stack' -%}
    <div class="pdp-gal">
      {%- for media in product.media limit: 7 -%}
        <figure>
          {%- if media.media_type == 'image' -%}
            <img src="{{ media | image_url: width: 1400 }}" alt="{{ media.alt | escape }}" width="1000" height="1250" loading="{% if forloop.first %}eager{% else %}lazy{% endif %}">
          {%- elsif media.media_type == 'video' -%}
            {{ media | video_tag: image_size: '1000x', controls: true, muted: true, loop: true, playsinline: true }}
          {%- else -%}
            {{ media | media_tag }}
          {%- endif -%}
        </figure>
      {%- else -%}
        <figure>{{ 'product-1' | placeholder_svg_tag: 'placeholder-svg' }}</figure>
      {%- endfor -%}
    </div>
    {%- else -%}
    <div class="pdp-gal pdp-gal--spot" data-gallery>
      <div class="pdp-stage" style="--stage-pad:{{ stage_pad }};--mosaic-w:{{ mosaic_w }}px">
        {%- if stills.size > 0 -%}
          {%- for media in stills limit: img_limit -%}
            <img class="pdp-shot{% if forloop.first %} on{% endif %}"
                 data-shot="{{ forloop.index0 }}"
                 src="{{ media | image_url: width: 1600 }}"
                 srcset="{{ media | image_url: width: 800 }} 800w, {{ media | image_url: width: 1200 }} 1200w, {{ media | image_url: width: 1600 }} 1600w"
                 sizes="(max-width:749px) 100vw, 55vw"
                 alt="{{ media.alt | escape }}" width="1000" height="1250"
                 loading="{% if forloop.first %}eager{% else %}lazy{% endif %}"
                 {% if forloop.first %}fetchpriority="high"{% endif %}>
          {%- endfor -%}
        {%- else -%}
          {%- assign ph = section.settings.stage_placeholder | default: 'product-1' -%}
          {%- capture ph_svg %}{{ ph | placeholder_svg_tag: 'placeholder-svg' }}{% endcapture -%}
          {{ ph_svg | replace: '<svg', '<svg preserveAspectRatio="xMidYMid slice"' }}
        {%- endif -%}

        {%- if stills.size > 1 and section.settings.show_mosaic != false -%}
          <div class="pdp-mosaic pdp-mosaic--{{ mosaic_pos }}" role="group" aria-label="{{ section.settings.mosaic_label | default: 'Product views' | escape }}">
            {%- for media in stills limit: img_limit -%}
              <button type="button" class="pdp-chip{% if forloop.first %} on{% endif %}" data-chip="{{ forloop.index0 }}"
                      aria-label="{{ section.settings.mosaic_label | default: 'View' }} {{ forloop.index }}">
                <img src="{{ media | image_url: width: 220 }}" alt="" width="110" height="138" loading="lazy">
              </button>
            {%- endfor -%}
          </div>
        {%- endif -%}
      </div>

      {%- if motion_place == 'below' and section.settings.show_motion != false -%}
        {%- if films.size > 0 or ext_films.size > 0 -%}
        <div class="pdp-motion" style="--motion-w:{{ motion_w }}%">
          {%- if section.settings.motion_label != blank -%}
            <span class="pdp-motion-h eyebrow">{{ section.settings.motion_label }}</span>
          {%- endif -%}
          <div class="pdp-motion-frame">
            {%- if films.size > 0 -%}
              {%- assign film = films | first -%}
              {{ film | video_tag:
                 image_size: '900x',
                 autoplay: section.settings.motion_autoplay,
                 loop: true,
                 muted: true,
                 controls: section.settings.motion_controls,
                 playsinline: true,
                 preload: 'metadata' }}
            {%- else -%}
              {%- assign film = ext_films | first -%}
              {{ film | external_video_tag }}
            {%- endif -%}
          </div>
        </div>
        {%- endif -%}
      {%- endif -%}
    </div>
    {%- endif -%}

    <div class="pdp-side">
      <div>
        <h1 class="dsp dsp-m" style="letter-spacing:.06em">{{ product.title }}</h1>
        <div class="pdp-price" data-price style="margin-top:10px">
          {%- assign cap = 0 -%}
          {%- if product.compare_at_price -%}{%- assign cap = product.compare_at_price | plus: 0 -%}{%- endif -%}
          {%- if cap > product.price -%}<s style="opacity:.55">{{ product.compare_at_price | money }}</s>&nbsp;{%- endif -%}
          {{ product.price | money }}
        </div>
      </div>

      {%- if product.metafields.custom.short_description != blank -%}
        <p class="small" style="max-width:46ch">{{ product.metafields.custom.short_description }}</p>
      {%- endif -%}

      <div data-variant-picker>
      {% form 'product', product %}
        <script type="application/json" data-variants>
          [{%- for variant in product.variants -%}
            {"id":{{ variant.id }},"options":{{ variant.options | json }},"available":{{ variant.available }},"price_formatted":{{ variant.price | money | json }}}{%- unless forloop.last -%},{%- endunless -%}
          {%- endfor -%}]
        </script>

        {%- for opt in product.options_with_values -%}
          {%- assign oname = opt.name | downcase -%}
          <div style="margin-bottom:18px">
            <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:10px">
              <span class="ui" style="color:var(--muted)">{{ opt.name }}{% if oname contains 'colour' or oname contains 'color' %} — <span data-colour-label>{{ opt.selected_value }}</span>{% endif %}</span>
              {%- if oname contains 'size' and section.settings.size_guide != blank -%}
                <a href="{{ section.settings.size_guide }}" class="ul ul-r" style="font-size:10.5px">Size Guide</a>
              {%- endif -%}
            </div>
            <div class="opt-row">
              {%- for value in opt.values -%}
                {%- assign is_swatch = false -%}
                {%- if value.swatch.color and oname contains 'col' -%}{%- assign is_swatch = true -%}{%- endif -%}
                <button type="button" class="opt{% if value == opt.selected_value %} on{% endif %}"
                        data-value="{{ value | escape }}"
                        {% if is_swatch %}style="padding:0;width:38px;height:38px;border-radius:50%;background:{{ value.swatch.color }};box-shadow:0 0 0 1px rgba(0,0,0,.12) inset" aria-label="{{ value | escape }}"{% endif %}>
                  {%- unless is_swatch -%}{{ value }}{%- endunless -%}
                </button>
              {%- endfor -%}
            </div>
          </div>
        {%- endfor -%}

        <input type="hidden" name="id" value="{{ v.id }}">
        <button type="submit" class="btn btn-solid" data-add style="width:100%;text-align:center;margin-top:6px"{% unless v.available %} disabled{% endunless %}>
          {%- if v.available -%}Add to Bag{%- else -%}Sold Out{%- endif -%}
        </button>
      {% endform %}
      </div>

      {%- if section.settings.fitting_link != blank -%}
        <a href="{{ section.settings.fitting_link }}" class="btn" style="width:100%;text-align:center;display:block">Book a Fitting</a>
      {%- endif -%}

      <div style="margin-top:14px">
        {%- if product.description != blank -%}
          <div class="acc"><button type="button" class="acc-h">Details &amp; Fit <span>+</span></button>
            <div class="acc-b"><div class="rte" style="padding-bottom:4px">{{ product.description }}</div></div></div>
        {%- endif -%}
        {%- for block in section.blocks -%}
          <div class="acc" {{ block.shopify_attributes }}>
            <button type="button" class="acc-h">{{ block.settings.title }} <span>+</span></button>
            <div class="acc-b"><div class="rte">{{ block.settings.body }}</div></div>
          </div>
        {%- endfor -%}
      </div>
    </div>
  </div>
</div>

{%- if section.settings.show_related -%}
<div class="wrap" style="padding-bottom:clamp(50px,6vw,90px)">
  <div class="row-head"><h2 class="dsp dsp-s">Complete the Look</h2></div>
  <div class="grid-4 stag in">
    {%- assign rel = section.settings.related_collection.products | default: collection.products -%}
    {%- assign n = 0 -%}
    {%- for p in rel -%}
      {%- if p.id != product.id and n < 4 -%}
        {% render 'product-card',
           product: p,
           index: forloop.index,
           ratio: section.settings.card_ratio,
           placeholder: section.settings.card_placeholder,
           fallback: section.settings.card_fallback,
           hover: section.settings.card_hover,
           align: section.settings.card_align,
           quick_label: section.settings.quick_label,
           swatch_limit: section.settings.swatch_limit,
           show_badge: section.settings.show_badge,
           show_fav: section.settings.show_fav,
           show_quick: section.settings.show_quick,
           show_price: section.settings.show_price,
           show_vendor: section.settings.show_vendor,
           show_swatches: section.settings.show_swatches,
           show_colour: section.settings.show_colour %}
        {%- assign n = n | plus: 1 -%}
      {%- endif -%}
    {%- endfor -%}
  </div>
</div>
{%- endif -%}

{% render 'section-copy', s: section, place: 'bottom' %}

{% schema %}
{
  "name": "Product",
  "settings": [
    {
      "type": "header",
      "content": "Gallery"
    },
    {
      "type": "select",
      "id": "gallery_style",
      "label": "Layout",
      "info": "Spotlight puts one large picture up front with a small mosaic of the others over its corner. Stack shows every picture down the page.",
      "options": [
        { "value": "spotlight", "label": "Spotlight with mosaic" },
        { "value": "stack", "label": "Stacked pictures" }
      ],
      "default": "spotlight"
    },
    {
      "type": "select",
      "id": "stage_ratio",
      "label": "Main picture shape",
      "options": [
        { "value": "1-1", "label": "Square" },
        { "value": "4-5", "label": "Portrait 4:5" },
        { "value": "3-4", "label": "Portrait 3:4" },
        { "value": "2-3", "label": "Tall 2:3" }
      ],
      "default": "4-5"
    },
    {
      "type": "text",
      "id": "stage_placeholder",
      "label": "Placeholder artwork",
      "info": "Shown only while a product has no pictures. e.g. product-1",
      "default": "product-1"
    },
    {
      "type": "range",
      "id": "image_limit",
      "label": "Pictures shown",
      "info": "0 shows every picture on the product.",
      "min": 0,
      "max": 12,
      "step": 1,
      "default": 0
    },
    {
      "type": "checkbox",
      "id": "show_mosaic",
      "label": "Show the mosaic switcher",
      "default": true
    },
    {
      "type": "select",
      "id": "mosaic_pos",
      "label": "Mosaic corner",
      "options": [
        { "value": "tr", "label": "Top right" },
        { "value": "tl", "label": "Top left" },
        { "value": "br", "label": "Bottom right" },
        { "value": "bl", "label": "Bottom left" }
      ],
      "default": "tr"
    },
    {
      "type": "range",
      "id": "mosaic_width",
      "label": "Mosaic size",
      "min": 90,
      "max": 240,
      "step": 6,
      "unit": "px",
      "default": 138
    },
    {
      "type": "text",
      "id": "mosaic_label",
      "label": "Mosaic label for screen readers",
      "default": "View"
    },
    {
      "type": "header",
      "content": "In motion"
    },
    {
      "type": "checkbox",
      "id": "show_motion",
      "label": "Show the film card",
      "info": "Plays the product video on its own, smaller card instead of inside the picture stack.",
      "default": true
    },
    {
      "type": "select",
      "id": "motion_place",
      "label": "Film sits",
      "options": [
        { "value": "below", "label": "Below the main picture" },
        { "value": "off", "label": "Nowhere — hide it" }
      ],
      "default": "below"
    },
    {
      "type": "range",
      "id": "motion_width",
      "label": "Film width",
      "min": 24,
      "max": 100,
      "step": 2,
      "unit": "%",
      "default": 46
    },
    {
      "type": "text",
      "id": "motion_label",
      "label": "Film caption",
      "default": "In motion"
    },
    {
      "type": "checkbox",
      "id": "motion_autoplay",
      "label": "Play the film automatically",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "motion_controls",
      "label": "Show film controls",
      "default": false
    },
    {
      "type": "url",
      "id": "size_guide",
      "label": "Size guide link"
    },
    {
      "type": "url",
      "id": "fitting_link",
      "label": "Book a fitting link"
    },
    {
      "type": "checkbox",
      "id": "show_related",
      "label": "Show 'Complete the Look'",
      "default": true
    },
    {
      "type": "collection",
      "id": "related_collection",
      "label": "Related products collection"
    },
    {
      "type": "header",
      "content": "Product cards"
    },
    {
      "type": "select",
      "id": "card_ratio",
      "label": "Image shape",
      "default": "4-5",
      "options": [
        {
          "value": "1-1",
          "label": "Square"
        },
        {
          "value": "4-5",
          "label": "Portrait 4:5"
        },
        {
          "value": "3-4",
          "label": "Portrait 3:4"
        },
        {
          "value": "2-3",
          "label": "Tall 2:3"
        },
        {
          "value": "16-9",
          "label": "Landscape 16:9"
        }
      ]
    },
    {
      "type": "select",
      "id": "card_hover",
      "label": "On hover",
      "default": "video",
      "options": [
        {
          "value": "video",
          "label": "Play product video, else second image"
        },
        {
          "value": "image",
          "label": "Show second image"
        },
        {
          "value": "zoom",
          "label": "Zoom the first image"
        },
        {
          "value": "none",
          "label": "Nothing"
        }
      ]
    },
    {
      "type": "select",
      "id": "card_align",
      "label": "Card text alignment",
      "default": "left",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Centre"
        }
      ]
    },
    {
      "type": "select",
      "id": "card_placeholder",
      "label": "Placeholder style",
      "default": "",
      "info": "Shown for products with no photograph yet.",
      "options": [
        {
          "value": "",
          "label": "Rotate through Shopify's set"
        },
        {
          "value": "product-1",
          "label": "Product 1"
        },
        {
          "value": "product-2",
          "label": "Product 2"
        },
        {
          "value": "product-3",
          "label": "Product 3"
        },
        {
          "value": "product-4",
          "label": "Product 4"
        },
        {
          "value": "product-5",
          "label": "Product 5"
        },
        {
          "value": "product-6",
          "label": "Product 6"
        },
        {
          "value": "lifestyle-1",
          "label": "Lifestyle 1"
        },
        {
          "value": "lifestyle-2",
          "label": "Lifestyle 2"
        },
        {
          "value": "tile",
          "label": "Plain tinted tile"
        }
      ]
    },
    {
      "type": "text",
      "id": "card_fallback",
      "label": "Bundled fallback image",
      "info": "Theme asset filename. Overrides the placeholder style."
    },
    {
      "type": "checkbox",
      "id": "show_badge",
      "label": "Show badge",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_fav",
      "label": "Show save button",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_quick",
      "label": "Show quick-view label",
      "default": true
    },
    {
      "type": "text",
      "id": "quick_label",
      "label": "Quick-view label",
      "default": "View"
    },
    {
      "type": "checkbox",
      "id": "show_price",
      "label": "Show price",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_vendor",
      "label": "Show vendor",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_swatches",
      "label": "Show colour swatches",
      "default": true
    },
    {
      "type": "range",
      "id": "swatch_limit",
      "label": "Swatches before \"+n\"",
      "min": 2,
      "max": 8,
      "step": 1,
      "default": 5
    },
    {
      "type": "checkbox",
      "id": "show_colour",
      "label": "Show colour name",
      "default": true
    },
    {
      "type": "header",
      "content": "Appearance"
    },
    {
      "type": "select",
      "id": "scheme",
      "label": "Colour scheme",
      "default": "light",
      "options": [
        {
          "value": "light",
          "label": "Bone"
        },
        {
          "value": "paper",
          "label": "Paper"
        },
        {
          "value": "tinted",
          "label": "Tinted"
        },
        {
          "value": "dark",
          "label": "Dark"
        },
        {
          "value": "gold",
          "label": "Gold"
        },
        {
          "value": "custom",
          "label": "Custom"
        }
      ]
    },
    {
      "type": "color",
      "id": "bg_custom",
      "label": "Custom background",
      "default": "#FAF8F5"
    },
    {
      "type": "color",
      "id": "text_custom",
      "label": "Custom text",
      "default": "#1B1A18"
    },
    {
      "type": "color",
      "id": "muted_custom",
      "label": "Custom secondary text",
      "default": "#8A837A"
    },
    {
      "type": "color",
      "id": "line_custom",
      "label": "Custom rule",
      "default": "#E4DED4"
    },
    {
      "type": "select",
      "id": "width",
      "label": "Content width",
      "default": "wide",
      "options": [
        {
          "value": "full",
          "label": "Full bleed"
        },
        {
          "value": "wide",
          "label": "Wide"
        },
        {
          "value": "narrow",
          "label": "Narrow"
        },
        {
          "value": "reading",
          "label": "Reading"
        }
      ]
    },
    {
      "type": "range",
      "id": "pad_top",
      "label": "Top padding",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "pad_bottom",
      "label": "Bottom padding",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 0
    },
    {
      "type": "checkbox",
      "id": "divider_top",
      "label": "Rule above",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "divider_bottom",
      "label": "Rule below",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "animate",
      "label": "Reveal on scroll",
      "default": true
    },
    {
      "type": "header",
      "content": "Light from above"
    },
    {
      "type": "checkbox",
      "id": "light_top",
      "label": "Light from above",
      "default": false,
      "info": "A soft overhead wash across this section. Looks best on dark schemes."
    },
    {
      "type": "color",
      "id": "light_color",
      "label": "Light colour",
      "default": "#FFFFFF"
    },
    {
      "type": "select",
      "id": "light_blend",
      "label": "Light blend",
      "default": "normal",
      "options": [
        {
          "value": "normal",
          "label": "Plain — a white wash"
        },
        {
          "value": "soft-light",
          "label": "Soft — barely there"
        },
        {
          "value": "screen",
          "label": "Bright — a strong beam"
        },
        {
          "value": "overlay",
          "label": "Contrast"
        }
      ]
    },
    {
      "type": "range",
      "id": "light_strength",
      "label": "Strength",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "%",
      "default": 30
    },
    {
      "type": "range",
      "id": "light_spread",
      "label": "Spread",
      "min": 40,
      "max": 200,
      "step": 10,
      "unit": "%",
      "default": 110
    },
    {
      "type": "range",
      "id": "light_pos",
      "label": "Position across",
      "min": 0,
      "max": 100,
      "step": 5,
      "unit": "%",
      "default": 50
    },
    {
      "type": "range",
      "id": "light_height",
      "label": "Reach down",
      "min": 20,
      "max": 140,
      "step": 10,
      "unit": "%",
      "default": 70
    },
    {
      "type": "header",
      "content": "Section background",
      "info": "A photograph or video behind everything in this section."
    },
    {
      "type": "select",
      "id": "bg_media_type",
      "label": "Background",
      "default": "none",
      "options": [
        {
          "value": "none",
          "label": "None"
        },
        {
          "value": "image",
          "label": "Image"
        },
        {
          "value": "video",
          "label": "Video"
        }
      ]
    },
    {
      "type": "image_picker",
      "id": "bg_image",
      "label": "Background image"
    },
    {
      "type": "image_picker",
      "id": "bg_image_mobile",
      "label": "Background image — mobile"
    },
    {
      "type": "video",
      "id": "bg_video",
      "label": "Background video (uploaded)"
    },
    {
      "type": "text",
      "id": "bg_video_url",
      "label": "Background video file URL",
      "info": "Direct .mp4 or .webm link."
    },
    {
      "type": "video_url",
      "id": "bg_embed_url",
      "label": "YouTube or Vimeo background",
      "accept": [
        "youtube",
        "vimeo"
      ]
    },
    {
      "type": "select",
      "id": "bg_fit",
      "label": "Background fit",
      "default": "cover",
      "options": [
        {
          "value": "cover",
          "label": "Cover"
        },
        {
          "value": "contain",
          "label": "Contain"
        }
      ]
    },
    {
      "type": "select",
      "id": "bg_focal",
      "label": "Background focal point",
      "default": "center",
      "options": [
        {
          "value": "center",
          "label": "Centre"
        },
        {
          "value": "top",
          "label": "Top"
        },
        {
          "value": "bottom",
          "label": "Bottom"
        },
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ]
    },
    {
      "type": "range",
      "id": "bg_overlay",
      "label": "Background overlay",
      "min": 0,
      "max": 90,
      "step": 5,
      "unit": "%",
      "default": 40
    },
    {
      "type": "color",
      "id": "bg_overlay_color",
      "label": "Overlay colour",
      "default": "#1B1A18"
    },
    {
      "type": "range",
      "id": "bg_blur",
      "label": "Background blur",
      "min": 0,
      "max": 24,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "checkbox",
      "id": "bg_fixed",
      "label": "Hold the background still while scrolling",
      "default": false
    },
    {
      "type": "header",
      "content": "Extra copy",
      "info": "An optional band of text and buttons, above or below this section."
    },
    {
      "type": "checkbox",
      "id": "copy_show",
      "label": "Show extra copy",
      "default": false
    },
    {
      "type": "select",
      "id": "copy_pos",
      "label": "Position",
      "default": "top",
      "options": [
        {
          "value": "top",
          "label": "Above the section"
        },
        {
          "value": "bottom",
          "label": "Below the section"
        }
      ]
    },
    {
      "type": "text",
      "id": "copy_eyebrow",
      "label": "Eyebrow"
    },
    {
      "type": "text",
      "id": "copy_heading",
      "label": "Heading"
    },
    {
      "type": "select",
      "id": "copy_heading_size",
      "label": "Heading size",
      "default": "dsp-m",
      "options": [
        {
          "value": "dsp-s",
          "label": "Small"
        },
        {
          "value": "dsp-m",
          "label": "Medium"
        },
        {
          "value": "dsp-l",
          "label": "Large"
        },
        {
          "value": "dsp-xl",
          "label": "Extra large"
        }
      ]
    },
    {
      "type": "text",
      "id": "copy_sub",
      "label": "Sub-heading"
    },
    {
      "type": "richtext",
      "id": "copy_body",
      "label": "Body",
      "default": "<p>Add a paragraph here.</p>"
    },
    {
      "type": "select",
      "id": "copy_align",
      "label": "Alignment",
      "default": "left",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Centre"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ]
    },
    {
      "type": "range",
      "id": "copy_width",
      "label": "Text width",
      "min": 40,
      "max": 100,
      "step": 5,
      "unit": "%",
      "default": 100
    },
    {
      "type": "text",
      "id": "btn1_text",
      "label": "Button 1 label"
    },
    {
      "type": "url",
      "id": "btn1_link",
      "label": "Button 1 link"
    },
    {
      "type": "select",
      "id": "btn1_style",
      "label": "Button 1 style",
      "default": "button",
      "options": [
        {
          "value": "button",
          "label": "Outline"
        },
        {
          "value": "solid",
          "label": "Solid"
        },
        {
          "value": "underline",
          "label": "Underline"
        }
      ]
    },
    {
      "type": "text",
      "id": "btn2_text",
      "label": "Button 2 label"
    },
    {
      "type": "url",
      "id": "btn2_link",
      "label": "Button 2 link"
    },
    {
      "type": "select",
      "id": "btn2_style",
      "label": "Button 2 style",
      "default": "underline",
      "options": [
        {
          "value": "underline",
          "label": "Underline"
        },
        {
          "value": "button",
          "label": "Outline"
        },
        {
          "value": "solid",
          "label": "Solid"
        }
      ]
    }
  ],
  "blocks": [
    {
      "type": "accordion",
      "name": "Accordion",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Fabric & Care"
        },
        {
          "type": "richtext",
          "id": "body",
          "label": "Body",
          "default": "<p>Dry clean only. Cool iron on the reverse.</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Product"
    }
  ]
}
{% endschema %}
